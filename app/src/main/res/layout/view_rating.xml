<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.android.tu.circlelibrary.CirclePercentBar
        android:id="@+id/circle_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:layout_margin="20dp"
        app:arcColor="@color/x_blue"
        app:arcStartColor="@color/x_green"
        app:arcWidth="15dp"
        app:centerTextColor="@color/translucent_black_15"
        app:centerTextSize="30sp"
        app:circleRadius="80dp" />

    <TextView
        android:id="@+id/textDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/circle_bar"
        android:gravity="center"
        android:text="@string/app_name"
        android:textColor="@color/translucent_black_50" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="15dp">

    </LinearLayout>

</RelativeLayout>